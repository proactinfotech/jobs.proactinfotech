# ─── Supabase (Vite frontend) ─────────────────────────────────────────────────
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-public-key

# ─── Supabase (Vercel serverless functions) ───────────────────────────────────
# Same values as above — process.env is used instead of import.meta.env
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key

# ─── Vercel Cron Security ─────────────────────────────────────────────────────
# Set this in Vercel Dashboard → Settings → Environment Variables.
# Vercel automatically injects it as `Authorization: Bearer <CRON_SECRET>` on cron calls.
# Generate with: openssl rand -base64 32
CRON_SECRET=your-random-secret-here
